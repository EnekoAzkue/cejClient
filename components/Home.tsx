import ScreenContainer from './ScreenContainer';
import { Text } from 'react-native';

const Home = () => {
  return (
    <ScreenContainer>
      <Text>Home Screen</Text>
    </ScreenContainer>
  );
};

export default Home;
